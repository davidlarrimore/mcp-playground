TIME_MCP_PORT=2001
FILESYSTEM_MCP_PORT=2002
MEMORY_MCP_PORT=2003
EMAIL_MCP_PORT=2004
MAILHOG_WEB_PORT=2005
MAILHOG_SMTP_PORT=2025
MCPO_PORT=2010

# Open WebUI + LiteLLM run externally; keep these for reference
OPENWEBUI_PORT=4000
LITELLM_PORT=7777

# Email MCP + MailHog wiring
SMTP_HOST=mailhog
SMTP_PORT=1025
SMTP_FROM_DEFAULT=noreply@example.test
ATTACH_ROOT=/attachments
LOG_LEVEL=INFO

# Time defaults
LOCAL_TIMEZONE=America/New_York

# Filesystem MCP allowed path inside the container
FILESYSTEM_ROOT=/demo-data
